<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/">Jekyll</generator>
  <link href="{{ site.url }}/{{ page.lang }}/feed.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url }}/{{ page.lang }}/" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/{{ page.lang }}/feed.xml</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
{%- assign lang_posts = site.articles | where: "lang", page.lang | where_exp: "p", "p.hidden != true" | sort: "date" | reverse -%}
{%- for post in lang_posts limit: 20 %}
  <entry xml:lang="{{ page.lang }}">
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url }}</id>
    <content type="html">{{ post.content | xml_escape }}</content>
    <summary type="html">{{ post.description | xml_escape }}</summary>
{%- if post.category %}
    <category term="{{ post.category | xml_escape }}"/>
{%- endif -%}
{%- for tag in post.tags %}
    <category term="{{ tag | xml_escape }}"/>
{%- endfor %}
  </entry>
{%- endfor %}
</feed>
